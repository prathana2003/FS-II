Developed a modern and responsive web page using React (Vite) following a component-based architecture.

Used Bootstrap for building the overall layout, navigation bar, grid system, spacing, and responsive design.

Integrated Material UI (MUI) to create reusable and visually consistent UI components such as cards and typography.

Designed the application with a hybrid UI approach, ensuring Bootstrap and MUI are used for different purposes to avoid conflicts.

Implemented a clean and organized folder structure separating components and pages for better readability and maintainability.

Created reusable components such as Navbar, Hero Section, Card Component, and Footer.

Ensured the web page is responsive and works smoothly on mobile, tablet, and desktop devices.

Applied modern UI/UX principles including proper spacing, typography, alignment, and visual hierarchy.

Demonstrated effective component reuse by displaying multiple cards using a single reusable Material UI card component.

Improved code quality and debugging skills by resolving import errors, component mismatches, and render issues.

Gained hands-on experience in combining multiple UI libraries in a single React project.

Built a meaningful real-world webpage suitable for use as a startup landing page or portfolio homepage.